# Історія змін

Усі помітні зміни в цьому проекті будуть задокументовані в цьому файлі.

## [2.2.0] - 2025-12-22

### Додано
- **Система інтернаціоналізації (i18n)**:
  - Реалізовано повну підтримку i18n за допомогою бібліотеки `react-i18next`.
  - Додано визначення мови з localStorage та налаштувань браузера.
  - Створено 10 файлів перекладу (українська та англійська) у 5 просторах імен (common, ui, notifications, errors, schedule).
  - Додано типізовані переклади TypeScript за допомогою `CustomTypeOptions`.
  - Реалізовано хук `useLanguageSync` для автоматичної синхронізації з бекендом через заголовок `Accept-Language`.

- **Компонент перемикання мов**:
  - Створено двомовний перемикач (десктопне випадаюче меню + мобільний список).
  - Інтегровано в десктопний хедер та мобільне бургер-меню.
  - Підтримка української 🇺🇦 та англійської 🇬🇧 мов з іконками прапорів.
  - Збереження обраної мови в localStorage.

- **Мобільне бургер-меню**:
  - Додано повноекранне мобільне меню навігації з плавною анімацією появи.
  - Включає розділ "Налаштування" (вибір мови, перемикання теми, сповіщення).
  - Включає навігаційні посилання (GitHub API, GitHub WEB).
  - Включає розділ "Інформація" з відображенням версії та переглядом історії змін.
  - Реалізовано захоплення фокусу для доступності клавіатури.
  - Підтримка закриття клавішею Escape.
  - Додано акордеон для вибору мови з анімованим розкриттям.

- **Перегляд історії змін**:
  - Створено інтерактивне модальне вікно історії змін з парсингом markdown.
  - Відображення історії версій з відформатованими розділами (Додано, Покращено, Виправлено, Змінено).
  - Відображення бейджів версій та дат релізів.
  - Доступ з розділу "Інформація" бургер-меню.
  - Адаптивний дизайн, оптимізований для мобільних пристроїв.

- **Уніфікована система анімацій**:
  - Реалізовано плавну анімацію `slideInRight` (0.25s cubic-bezier) для бургер-меню.
  - Оновлено модальне вікно NotificationCenter для використання анімації `modalFadeScale`.
  - Мобільні модальні вікна використовують миттєві переходи для кращого UX.
  - Усі анімації використовують апаратно прискорені трансформації GPU.

### Покращено
- **Оптимізація продуктивності**:
  - Створено універсальну CSS-змінну `--glass-blur` для запобігання перемальовуванню (repaint) під час зміни теми.
  - Усі компоненти `glass-card` тепер використовують спільну змінну розмиття (`blur(16px)`).
  - Видалено `backdrop-filter: blur()` з фонів модальних вікон/меню для зниження навантаження на GPU.
  - Додано `will-change: transform, opacity` для анімованих компонентів.
  - Додано `transform: translate3d(0, 0, 0)` для просування шарів GPU.
  - Додано `contain: content` для ізоляції макета.
  - Реалізовано класи `body.modal-open` та `body.menu-open` для вимкнення розмиття на фоновому контенті.

- **Переклади компонентів**:
  - Перекладено всі тексти інтерфейсу в App.tsx, Header.tsx, Footer.tsx.
  - Перекладено понад 30 хардкод-текстів у NotificationCenter.tsx.
  - Усі повідомлення сповіщень тепер підтримують як українську, так і англійську мови.
  - Екстрені сповіщення тепер показують локалізовані заголовки.

- **Мобільний UX**:
  - Бургер-меню тепер займає всю ширину екрана (100%) замість 85%.
  - Висота заголовка модального вікна збільшена до 4rem на мобільних пристроях для зручності натискання.
  - Додано підтримку кнопки "Назад" у NotificationCenter для мобільної навігації.
  - Перемикач мов у бургер-меню показує поточний вибір за допомогою галочки.
  - Додано бейдж непрочитаних сповіщень на кнопку бургер-меню та всередині меню для пункту сповіщень.

### Виправлено
- **Оновлення безпеки React**:
  - Оновлено React з 19.2.0 до 19.2.3 як запобіжний захід проти CVE-2025-55182, CVE-2025-55184, CVE-2025-67779.
  - Попередня версія не використовувала вразливі функції, але оновлено згідно з кращими практиками безпеки.
  - Задокументовано в README.md.

- **Ключі перекладу**:
  - Виправлено непослідовні ключі перекладу в NotificationCenter (наприклад, `subscriptionActivatedMessage` → `subscriptionActivatedDesc`).
  - Оновлено переклади графіку відповідно до формату повідомлень бекенду.
  - Додано підтримку інтерполяції для динамічних значень (час, дата) у повідомленнях сповіщень.

### Змінено
- Фони модальних вікон та бургер-меню більше не використовують `backdrop-filter` для кращої продуктивності.
- Ефект розмиття glass-card тепер використовує CSS-змінну для консистентності між темами.
- NotificationCenter тепер можна використовувати в керованому режимі за допомогою пропсів `isOpen` та `onOpenChange`.
- Мобільна версія перемикача мов інтегрована безпосередньо в акордеон бургер-меню.

---

## [2.1.0] - 2025-12-21

### Додано
- **Глобальна оптимізація продуктивності**: 
  - Реалізовано апаратне прискорення GPU для всіх анімацій модальних вікон та меню за допомогою `will-change` та `translate3d`.
  - Додано ізоляцію макета (`contain: content`) для анімованих компонентів для зменшення області перерахунку.
- **Оптимізація зміни теми**:
  - Реалізовано логіку `no-transitions` для запобігання "штормам перемальовування" під час перемикання між світлою та темною темами.
  - Зменшено тривалість переходу фону при зміні теми для більш швидкого відгуку.
- **Керування фоновими ресурсами**:
  - Реалізовано динамічну оптимізацію фону: `backdrop-filter` та `filter` автоматично вимикаються на головному `app-root`, коли активні важкі компоненти інтерфейсу (модальні вікна/меню).
- **Документація**:
  - Додано вичерпний [Посібник з оптимізації](docs/optimization-guide.md) для майбутнього обслуговування та навчання.

### Покращено
- Анімації відкриття модальних вікон стали значно плавнішими (60FPS) як на десктопних, так і на мобільних пристроях.
- Зміна теми тепер відбувається миттєво на мобільних пристроях без затримок інтерфейсу.

---

## [2.0.0] - 2025-12-10

### Додано
- Початковий реліз оновленого Blackout Calendar WEB.
- Підтримка декількох черг.
- Система push-сповіщень.
- Багатомовна підтримка (UK/EN).
