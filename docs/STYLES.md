# Styles Structure

Модульна структура CSS для проєкту Blackout Calendar.

## 📁 Структура

```
src/styles/
├── index.css              # Головний файл - імпортує всі модулі
├── variables.css          # CSS змінні для light/dark теми
├── base.css              # Reset, body, utility classes
└── components/           # Стилі компонентів
    ├── header.css        # App header, logo, offline banner
    ├── clock.css         # Clock & date selector
    ├── queue.css         # Queue selector grid
    ├── visualization.css # Visualization card & states
    ├── timeline.css      # Timeline component
    ├── buttons.css       # Icon buttons, badges
    ├── modal.css         # Modal, notifications, tabs, settings
    └── footer.css        # Footer & GitHub links
```

## 🎨 Як використовувати

### Імпорт всіх стилів
```tsx
// В src/index.tsx
import './styles/index.css';
```

### Робота з окремими модулями
Кожен модуль незалежний і може бути оновлений окремо:

- **variables.css** - змінюйте кольори, відступи, тіні
- **base.css** - базові стилі та utility classes
- **components/*.css** - стилі конкретних компонентів

## ⚙️ Build процес

При build всі модулі об'єднуються в один оптимізований CSS файл:
- PostCSS додає vendor prefixes
- Cssnano мініфікує CSS
- Vite оптимізує і додає hash до імені файлу

## 📝 Правила редагування

1. **Змінні** - завжди використовуйте CSS змінні з `variables.css`
2. **Компоненти** - тримайте стилі компоненту в одному файлі
3. **Порядок** - не змінюйте порядок імпортів в `index.css`
4. **Префікси** - не додавайте вручну, autoprefixer зробить це автоматично

## 🚀 Переваги модульної структури

✅ Легше знаходити потрібні стилі
✅ Простіше вносити зміни
✅ Зменшує конфлікти при роботі в команді
✅ Покращує підтримку коду
✅ Дозволяє завантажувати тільки потрібні стилі
